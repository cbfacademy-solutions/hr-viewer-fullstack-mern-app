{
  "name": "memberview",
  "version": "1.0.0",
  "description": "Fullstack MERN app for reviewing users",
  "main": "app.js",
  "scripts": {
    "server": "nodemon server/app.js",
    "start": "node server/app.js",
    "client": "npm run start --prefix client",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev-install": "rm -rf node_modules npm install && cd client && rm -rf node_modules && npm install && cd ..",
    "develop": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\""
  },
  "author": "Efua Akumanyi (efuaakumanyi.com)",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.20.2",
    "dotenv": "^16.0.3",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongodb": "^5.1.0",
    "validation": "^0.0.1"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "nodemon": "^2.0.20"
  }
}
